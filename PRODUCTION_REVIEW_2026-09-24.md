# مراجعة الجاهزية للإنتاج — UV-Vis Spectrum Studio 3.1.1

تاريخ التحقق النهائي: 2026-09-24. هذا التقرير يحل محل حكم الجاهزية في المراجعة العلمية الأولى ذات 100 اختبار.

## الحكم

**مرشح إصدار بحثي محسّن ومختبر، وليس إصداراً معتمداً للإنتاج على جميع المنصات.**
النطاق المنفذ فعلياً: Python 3.12 على Linux x86_64، واختبارات الحسابات والأرشيفات والواجهة، ومسار عمل داخل Chromium حقيقي.
اختبارات مثبتات Windows وmacOS وبقية معماريات Linux لم تُنفّذ هنا؛ وجود ملفات CI لا يُعد نجاحاً لهذه الاختبارات.
لم تُرفع التغييرات إلى GitHub ولم يُنشر إصدار عام.

## ما أُصلح

- تصحيح أبعاد التكامل إلى Abs·nm أو Abs·Å؛ لا علاقة لقيمة التكامل الطيفي بمساحة المقطع العرضي للكيوفيت. تكامل المشتقة الرابعة على محور nm وحدته Abs/nm³.
- تقسيم المقاطع عند عبور الصفر لحساب المساحة المطلقة وعزم المساحة، واستيفاء حدود مجال التكامل.
- تثبيت حسابات OLS وWLS وMandel وBreusch–Pagan والتنبؤ العكسي عند تغيير وحدات التركيز أو الإشارة.
- إظهار R² غير معرّف للاستجابة الثابتة، وإظهار اختبار نقص المطابقة غير قابل للاستدلال عندما يكون الخطأ النقي صفراً، بدلاً من رفض زائف.
- حساب sample SD من البلانكات المستقلة باستخدام n−1، وإظهار مصدر σ وفترات الثقة والبواقي في المعايرة.
- تقييد مكونات PLS/PCR بحجم أصغر مجموعة تدريب، وإصلاح العينات المكررة في Kennard–Stone/SPXY وتطبيع مسافات SPXY.
- حفظ أول قراءة في الملفات النصية بلا ترويسة، ودعم الفاصلة العشرية والمسوح التي تبدأ عند 190 nm.
- فحص ZIP وNPZ ورؤوس NPY قبل تخصيص الذاكرة: حدود لفك الضغط والنقاط والأطياف، ومنع الأسماء المكررة والمسارات غير الآمنة.
- إلزام بيان SHA-256 بتغطية كل محتويات المشروع، والتحقق من إعدادات الواجهة قبل استعادتها. البصمة تكشف تلف المحتوى؛ لا تثبت هوية منشئه.
- إصلاح توقف مقارنة المشتقات، وتزامن مجال التكامل مع الرسم والتنزيل، واستعادة المحاور وإعدادات المشروع.
- إبطال الرسوم القديمة عند تغيير المعالجة أو إعدادات الرسم، وتصحيح تحديث عنوان المحور التلقائي عند تغيير رتبة المشتقة أو التحويل الضوئي.
- تصحيح تحجيم PNG: زيادة DPI ترفع دقة الشكل كله مع الحفاظ على نسب النص والخطوط؛ أضيف حد لحجم الصورة.
- إصلاح نقطة الدخول في الحزمة المثبتة؛ التشغيل المحلي الافتراضي يرتبط بـ127.0.0.1.
- تحديث Pillow إلى 12.3.0 وتعديل قيد الاعتماد للسماح بالإصلاحات الأمنية الحديثة؛ قراءة الصورة الناتجة مقصورة على PNG.
- توحيد بيانات الإصدار إلى 3.1.1 وتوسيع CI إلى Windows/Linux، وإضافة اختبار الحزمة المثبتة في المتصفح وفحص الاعتماديات.
- استبدال نشر الإصدار القديم التلقائي بإعداد مسودة يدوياً دون خطوة نشر عام تلقائي.

## أدلة التحقق

| البوابة | النتيجة الفعلية |
|---|---|
| الاختبارات العلمية والانحدارية | 130 ناجحاً؛ 0 فشل؛ 0 خطأ؛ 0 تخطٍ |
| تغطية السطور بـpytest | 64.85%، دون العمليات الفرعية للمتصفح |
| المتصفح من المصدر | ناجح: رفع وحساب ومشتقات وتصدير وحفظ/فتح |
| الحزمة المثبتة في بيئة جديدة | ناجح: نفس المسار عبر CLI خارج شجرة المصدر |
| PNG / SVG / PDF | تنزيل فعلي؛ PNG بحجم 2100×1500 و300 DPI |
| الاعتماديات | 54 اعتمادية تشغيل؛ 0 ثغرة معروفة؛ 0 اعتمادية متخطاة |
| wheel وsdist | البناء وTwine ناجحان؛ ملفات Python داخل wheel مطابقة للمصدر المختبر |
| مثبتات Windows وmacOS | غير منفّذة؛ تمنع اعتماد الإنتاج العام متعدد المنصات |


تفاصيل البوابات المنفذة محفوظة في `release_evidence/qualification.json`، مع JUnit XML ونتائج المتصفح وفحص الاعتماديات.
الاختبارات لا تقيس فقط مرور الدوال: توجد مراجع عددية مستقلة، وتحويلات وحدات، وحالات حدودية، وملفات متعمدة التلف.
مسار المتصفح يرفع طيفاً دون ترويسة، ويقارن المشتقات، ويحدد مجال تكامل، ويتحقق من وحدة المشتقة الرابعة داخل الرسم، وينزّل PNG/SVG/PDF، ويغيّر المعالجة للتحقق من إبطال الرسم السابق، ثم يحفظ المشروع ويفتحه في جلسة ثانية.

الطيف المرجعي: `0.02 + 0.8 exp(-0.5 ((λ-270)/12)^2)` بخطوة 1 nm.
تكامله التحليلي بين 260 و280 nm هو 14.726120259169024؛ عرض التطبيق 14.7196، بفارق نسبي يقارب 0.0443%، متوافق مع التكامل شبه المنحرف للشبكة المتقطعة. لم يُغيّر الاختبار لمساواة هذين العددين اصطناعياً.

تغطية pytest للسطور تقارب 65%؛ لا تشمل العمليات الفرعية للمتصفح. لا تعني تغطية كل خوارزمية وكل توليفة إعدادات.
فحص الاعتماديات يبحث في الثغرات المعروفة وقت التنفيذ، ولا يثبت غياب الثغرات غير المنشورة أو قابلية الاستغلال الفعلية داخل التطبيق.

## بوابات الاعتماد المتبقية

1. **المثبتات الأصلية:** اختبار بناء EXE/DMG/PyInstaller والتثبيت والتشغيل والتصدير والحفظ والإزالة على كل نظام مستهدف. لم تُنفّذ هذه البوابة في بيئة Linux الحالية.
2. **الاستضافة المشتركة:** لم تُختبر أحمال متعددة المستخدمين أو بيئة خدمة عامة أو اختبار اختراق. يلزم اختبار المصادقة وTLS وحدود الموارد والنسخ الاحتياطي في بيئة النشر المقصودة إذا كان الهدف خدمة مشتركة.
3. **الإجراء التحليلي:** لم تُقدّم ملفات أجهزة أو مواد مرجعية لإجراء مختبري محدد ضمن هذه المهمة. الأطياف التركيبية والمراجع العددية تتحقق من البرمجيات؛ اعتماد الطريقة يحتاج بيانات تجريبية مناسبة للغرض.
4. **حدود النطاق:** لا يُدّعى تأهيل GMP/GLP أو Part 11، ولا اختبار كل توليفات الخوارزميات أو متصفحات الهواتف.

## إعادة التنفيذ

داخل بيئة افتراضية جديدة من مجلد المصدر:

```bash
python -m pip install -r requirements.txt pytest pytest-cov build twine pip-audit playwright==1.51.0
python tools/validate_release_metadata.py
python -m pytest --junitxml=release_evidence/pytest.xml --cov=uvvis_studio
python -m build
python -m twine check dist/*
python -m pip_audit -r requirements-tested-linux-py312.txt --no-deps --disable-pip
python -m playwright install --with-deps chromium
python tools/production_browser_check.py
```

لاختبار wheel: أنشئ بيئة افتراضية ثانية، وثبّت الحزمة وPlaywright فيها، ونفّذ الأداة بمفسّرها مع `--installed --output /absolute/path/to/evidence`.
الأداة تتحقق من أن الاستيراد يأتي من الحزمة المثبتة خارج شجرة المصدر وتشغّل CLI فعلياً.
يمكن تحديد `BROWSER_PATH` لمتصفح Chrome/Chromium متوافق. متصفح الاختبارات ليس مضمناً في توزيع البرنامج.

`requirements-tested-linux-py312.txt` لقطة اعتماديات التشغيل المختبرة على Linux/Python 3.12؛ استخدمها مع `pip install -c` لتكرار هذه البيئة. ليست ملف قفل صالحاً تلقائياً لكل منصة.

## مراجع

- NIST/SEMATECH، فصل الخطأ النقي ونقص المطابقة: https://www.itl.nist.gov/div898/handbook/pmd/section4/pmd446.htm
- إصلاحات Pillow 12.3.0: https://pillow.readthedocs.io/en/stable/releasenotes/12.3.0.html
- المراجع العلمية للخوارزميات موثقة في `REFERENCES.md`.
